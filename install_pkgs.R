# cran packages
cran_pkgs <- c("tidyverse", "openair", "worldmet", "devtools", "learnr")

# install
install.packages(cran_pkgs)

# install github
devtools::install_github("mohowu/ricardor")
devtools::install_github("skgrange/saqgetr")
